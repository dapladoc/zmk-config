manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: kmobs
      url-base: https://github.com/kmobs
    - name: englmaxi
      url-base: https://github.com/englmaxi
    - name: dapladoc
      url-base: https://github.com/dapladoc
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: main
      import: app/west.yml
    - name: zmk-feature-softdevice-subrating
      remote: kmobs
      repo-path: zmk-softdevice-controller
      revision: main
      import: config/west.yml
    - name: zmk-dongle-display
      remote: dapladoc
      # Forked from englmaxi/zmk-dongle-display to add our own custom
      # status-screen widgets (last-key, keystroke-count, activity,
      # build-info -- see that fork's README/commits).
      # EXPERIMENT (2026-07-16): tracking main again now that zmk core is
      # also on upstream main -- main's zmk_endpoint_get_selected() should
      # match current ZMK's endpoint API. See
      # docs/superpowers/plans/2026-07-16-upstream-zmk-migration-experiment.md
      revision: main
  self:
    path: config
